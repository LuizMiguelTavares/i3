Needs to install the picom
sudo add-apt-repository ppa:regolith-linux/release
sudo apt update
sudo apt install picom

Needs to install the feh
sudo apt install feh

And then put an wallpaper named: wallpaper_dark.jpg
